diff --git a/node_modules/@deepseek-ai/dsh-workspace/lib/index.js b/node_modules/@deepseek-ai/dsh-workspace/lib/index.js
index 42486dc..cad819a 100644
--- a/node_modules/@deepseek-ai/dsh-workspace/lib/index.js
+++ b/node_modules/@deepseek-ai/dsh-workspace/lib/index.js
@@ -454,6 +454,29 @@ var WorkspaceRegistry = class extends Service {
 			});
 		});
 	}
+	/** Remove a permanently deleted Session from Workspace and archive state. */
+	forgetSession(sessionId) {
+		return this.enqueueOperation(async () => {
+			for (const [workspaceId, record] of this.requireTable().entries()) {
+				if (!record.sessionIds.includes(sessionId)) continue;
+				const entity = this.entities.get(workspaceId);
+				if (entity !== void 0) await entity.detachSession(sessionId);
+				else await this.requireTable().update(workspaceId, (current) => current.sessionIds.includes(sessionId) ? {
+					...current,
+					sessionIds: current.sessionIds.filter((id) => id !== sessionId)
+				} : current);
+				break;
+			}
+			const state = this.requireState();
+			if (state.archivedSessionIds.includes(sessionId)) await this.setState({
+				...state,
+				archivedSessionIds: state.archivedSessionIds.filter((id) => id !== sessionId)
+			});
+			this.headers.delete(sessionId);
+			this.sessionPaths.delete(sessionId);
+			this.invalidSessionPaths.delete(sessionId);
+		});
+	}
 	/**
 	* Whether a session is live, header-indexed, or present in a fresh
 	* persistence listing. Only a definite miss returns false — a failing
diff --git a/node_modules/@deepseek-ai/dsh-workspace/lib/types/index.d.ts b/node_modules/@deepseek-ai/dsh-workspace/lib/types/index.d.ts
index fc0fa1b..f22e468 100644
--- a/node_modules/@deepseek-ai/dsh-workspace/lib/types/index.d.ts
+++ b/node_modules/@deepseek-ai/dsh-workspace/lib/types/index.d.ts
@@ -122,6 +122,8 @@ export declare class WorkspaceRegistry extends Service {
      * @returns resolution after durability.
      */
     archiveSession(sessionId: SessionId): Promise<void>;
+    /** Remove a permanently deleted Session from Workspace and archive state. */
+    forgetSession(sessionId: SessionId): Promise<void>;
     /**
      * Whether a session is live, header-indexed, or present in a fresh
      * persistence listing. Only a definite miss returns false — a failing
diff --git a/node_modules/@deepseek-ai/dsh-workspace/lib/types/index.js b/node_modules/@deepseek-ai/dsh-workspace/lib/types/index.js
index 8140b89..06fcf0a 100644
--- a/node_modules/@deepseek-ai/dsh-workspace/lib/types/index.js
+++ b/node_modules/@deepseek-ai/dsh-workspace/lib/types/index.js
@@ -213,6 +213,32 @@ export class WorkspaceRegistry extends Service {
             await this.setState({ ...state, archivedSessionIds: [...state.archivedSessionIds, sessionId] });
         });
     }
+    /** Remove a permanently deleted Session from Workspace and archive state. */
+    forgetSession(sessionId) {
+        return this.enqueueOperation(async () => {
+            for (const [workspaceId, record] of this.requireTable().entries()) {
+                if (!record.sessionIds.includes(sessionId))
+                    continue;
+                const entity = this.entities.get(workspaceId);
+                if (entity !== undefined) await entity.detachSession(sessionId);
+                else await this.requireTable().update(workspaceId, (current) => current.sessionIds.includes(sessionId) ? {
+                    ...current,
+                    sessionIds: current.sessionIds.filter((id) => id !== sessionId)
+                } : current);
+                break;
+            }
+            const state = this.requireState();
+            if (state.archivedSessionIds.includes(sessionId)) {
+                await this.setState({
+                    ...state,
+                    archivedSessionIds: state.archivedSessionIds.filter(id => id !== sessionId),
+                });
+            }
+            this.headers.delete(sessionId);
+            this.sessionPaths.delete(sessionId);
+            this.invalidSessionPaths.delete(sessionId);
+        });
+    }
     /**
      * Whether a session is live, header-indexed, or present in a fresh
      * persistence listing. Only a definite miss returns false — a failing
