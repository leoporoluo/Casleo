import type { MarkdownTheme } from "@earendil-works/pi-tui";
/** Remove Pi's literal fence rows while retaining parsing and syntax highlighting. */
export declare function codexStyleMarkdownTheme(theme: MarkdownTheme): MarkdownTheme;
export declare function installPiMarkdownCodeBlocks(): void;
